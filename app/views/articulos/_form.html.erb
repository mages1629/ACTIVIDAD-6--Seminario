<%= form_with(model: articulo, local: true) do |form| %>
  <% if articulo.errors.any? %>
    <div class="error-messages">
      <h2><%= pluralize(articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>
      <ul>
        <% articulo.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :contenido %>
    <%= form.text_area :contenido, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit "Guardar", class: "btn" %>
  </div>
<% end %>

